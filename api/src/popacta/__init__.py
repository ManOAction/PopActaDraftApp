"""Draft-day copilot backend.

Phase 0: this package exists so the toolchain has something to lint, type and
test. The domain core (snake order, pick windows, roster slots) arrives in
Phase 1 under `popacta.domain`.
"""

__version__ = "0.1.0"
